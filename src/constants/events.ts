import { prefixCls } from 'constants/theme';

export const SET_GLOBAL_LOADING = `${prefixCls}-event-setGlobalLoading`;
export const SET_GLOBAL_SYNC_TIPS_MODAL = `${prefixCls}-event-setGlobalSyncTipsModal`;
